# Copy this file to .env and fill in a key to enable live LLM generation.
# If neither is set, the app runs fully offline using a deterministic
# template-based mock (see src/rewards_agent/llm_client.py). The agent graph
# and all business logic behave identically either way — only the
# notification / fraud-reasoning text generation quality changes.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
